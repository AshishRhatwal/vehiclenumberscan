<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mra.com.vehicletracker">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/l"
        android:label="@string/app_name"
        android:roundIcon="@drawable/l"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AdminMainActivity" />
        <activity android:name=".VehicleScanningActivity" />
        <activity android:name=".FetchingValuesOfCardDetails" />
        <activity android:name=".SendInfo" />
        <activity android:name=".AddPoliceAdminActivity" />
        <activity android:name=".BlackListedVehicleForAdmin" />
        <activity android:name=".AdminManagePolice" />
        <activity android:name=".AdminLoginAct" />
        <activity android:name=".PoliceLogin" />
        <activity android:name=".StartActivity" />
        <activity android:name=".SendAdmin"></activity>
    </application>

</manifest>